[project]
name = "12-finops-budget-guard-agent"
version = "0.1.0"
description = "Track spend-risk tradeoffs with policy and telemetry-backed execution analytics."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.12.5",
]
