[project]
name = "22-llm-builder-strategy-agent"
version = "0.1.0"
description = "Build a production LLM client with middleware and structured output for revenue forecasting."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.12.5",
]
