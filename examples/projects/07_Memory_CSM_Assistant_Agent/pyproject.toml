[project]
name = "07-memory-csm-assistant-agent"
version = "0.1.0"
description = "Maintain customer success context across turns and summarize continuity metrics."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.12.5",
]
