[project]
name = "18-fraud-investigation-orchestrator-agent"
version = "0.1.0"
description = "Run governance-grade fraud investigations with portfolio-level execution analytics."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.12.5",
]
