[project]
name = "03-support-sla-triage-agent"
version = "0.1.0"
description = "Prioritize support workload using SLA-aware triage analytics."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.12.5",
]
