[project]
name = "24-data-pipeline"
version = "0.1.0"
description = "A data pipeline orchestrator demonstrating DelegationPlan DAG execution"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []
