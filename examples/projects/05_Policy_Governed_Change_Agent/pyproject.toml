[project]
name = "05-policy-governed-change-agent"
version = "0.1.0"
description = "Gate production changes with policy rules and approval analytics."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.12.5",
]
