[project]
name = "17-experiment-insights-hub-agent"
version = "0.1.0"
description = "Synthesize experiment outcomes with scenario simulation and batch analytics."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.12.5",
]
