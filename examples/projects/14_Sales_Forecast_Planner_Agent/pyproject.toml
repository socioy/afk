[project]
name = "14-sales-forecast-planner-agent"
version = "0.1.0"
description = "Run batch forecast workflows with sqlite memory and compaction analytics."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.12.5",
]
