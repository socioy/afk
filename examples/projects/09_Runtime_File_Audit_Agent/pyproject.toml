[project]
name = "09-runtime-file-audit-agent"
version = "0.1.0"
description = "Audit policy and compliance files using prebuilt runtime tools and tool-level analytics."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.12.5",
]
