[project]
name = "09-compliance-file-inspector-agent"
version = "0.1.0"
description = "Inspect compliance workflows with richer multi-turn analytics patterns."
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pydantic>=2.12.5",
]
